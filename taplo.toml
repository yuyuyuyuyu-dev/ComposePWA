exclude = [
  "**/build/**",
  "**/node_modules/**",
  ".gradle/**",
  ".idea/**",
  ".kotlin/**",
  "kotlin-js-store/**",
  "example/**",
  "tmp/**",
]

[formatting]
# Sort version-catalog entries so they are easy to diff against other projects.
reorder_keys = true
