# Build outputs & tool caches
**/build/
.gradle/
.idea/
.kotlin/
.claude/
node_modules/

# NOTE: the web assets the ComposePWA plugin emits (index.html, manifest.json,
# registerServiceWorker.js, workbox-config-*.js) are deliberately NOT ignored.
# In a real consumer project they are committed source that must pass Prettier, so
# the "ComposePWA output" CI job generates and checks them like any other file.
# They are .gitignored here only so CI can confirm the plugin regenerates them.

# Lockfiles & Gradle wrapper
kotlin-js-store/
gradlew
gradlew.bat
gradle/wrapper/

# Handled by other tools (ktlint, taplo) or not formattable
*.kt
*.kts
*.toml
*.xml
LICENSE
