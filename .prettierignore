# Build outputs & tool caches
**/build/
.gradle/
.idea/
.kotlin/
.claude/
node_modules/

# Git-ignored copies the ComposePWA plugin generates into the sample app at
# build time. They are byte-for-byte copies of plugin/src/main/resources/* (which
# Prettier already checks) and are verified directly by the "ComposePWA output"
# job in .github/workflows/lint.yml. index.html is intentionally NOT listed: the
# plugin no longer rewrites it, so Prettier owns its formatting like any other file.
composeApp/workbox-config-for-*.js
composeApp/src/webMain/resources/manifest.json
composeApp/src/webMain/resources/registerServiceWorker.js
composeApp/src/webMain/resources/icons/

# Lockfiles & Gradle wrapper
kotlin-js-store/
gradlew
gradlew.bat
gradle/wrapper/

# Handled by other tools (ktlint, taplo) or not formattable
*.kt
*.kts
*.toml
*.xml
LICENSE
