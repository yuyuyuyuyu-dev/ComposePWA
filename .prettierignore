# Build outputs & tool caches
**/build/
.gradle/
.idea/
.kotlin/
.claude/
node_modules/

# The example app is a faithful, drop-in user project, kept out of the repo's general
# formatting so its wizard-generated files stay byte-for-byte authentic. The web assets
# the plugin emits/modifies there are verified separately by the "ComposePWA output" CI
# job (a targeted Prettier run + git diff). The plugin's SOURCE templates under
# plugin/src/main/resources/ are NOT ignored and must pass Prettier like any other file.
example/

# Local scratch workspace (its contents are git-ignored); never linted.
tmp/

# Lockfiles & Gradle wrapper
kotlin-js-store/
gradlew
gradlew.bat
gradle/wrapper/

# Handled by other tools (ktlint, taplo) or not formattable
*.kt
*.kts
*.toml
*.xml
LICENSE
